<h2>pictures</h2>

<% @pictures.each do |picture| %>
	<p>
		<%= picture[:title] %>, <%= picture[:artist] %>
		<img src="<%= picture[:url] %>">
	</p>

<% end %>

<%= link_to "Upload", pictures_new_path %>